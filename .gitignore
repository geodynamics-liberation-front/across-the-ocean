# Downloaded source datasets, the built site data and the assembled site.
# Regenerate with `make dist` (tools/fetch_data.sh downloads ~130 MB into
# sources/, tools/build_data.py writes site/data/, dist/ is a copy of site/).
/sources/
/site/data/
/dist/
/dist/

# Superseded originals kept locally only
/attic/

# Python / node / editor leftovers
__pycache__/
*.pyc
.venv/
node_modules/
.DS_Store
*.part
